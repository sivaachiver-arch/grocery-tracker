pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Build started'
            }
        }

        stage('Test') {
            steps {
                echo 'Test completed'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploy completed'
            }
        }
    }
}
